// app/(admin)/admin/draw/[raffleId]/page.tsx
export default function drawRaffleIdPage() {
  return (
    <main className="p-8">
      <h1 className="font-display text-3xl text-charcoal">Draw raffle Id Page</h1>
      <p className="text-sage text-sm mt-2 font-mono">
        This page isn't built yet (Phase 6) — this is a placeholder
        so the route compiles.
      </p>
    </main>
  )
}